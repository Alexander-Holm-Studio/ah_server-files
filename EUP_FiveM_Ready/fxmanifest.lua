fx_version 'cerulean'
game 'gta5'

-- Resource metadata
author 'EUP Pack'
description 'EUP - FiveM ready ped textures and models'
version '1.0.0'

-- Stream all nested files in the `stream` folder (all mp_m_* files have been moved/renamed into `stream`)
files {
    'stream/**/*.*'
}

-- No .meta files detected in the resource; add data_file lines here if you add any .meta files
-- Example: data_file 'PED_METADATA_FILE' 'stream/ped_metadata.meta'